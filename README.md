# Integration-Hub
REST API, file-backed inventory system, and pluggable email/SMS notifications coordinated through an in-memory event bus, with circuit breaker + retry protecting every external call. Zero dependencies.
